<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800;900&display=swap"
        rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
        }
    </style>
</head>

<body class="bg-slate-50">

    <%- include('../partials/navbar') %>

        <div class="pt-20 flex min-h-screen">
            <%- include('../partials/sidebar') %>

                <main class="flex-1 md:ml-64 p-8">
                    <div class="max-w-7xl mx-auto">

                        <!-- Header & Actions -->
                        <div class="flex flex-col md:flex-row justify-between items-center gap-4 mb-8">
                            <div>
                                <h2 class="text-3xl font-black text-slate-800">Voluntarios</h2>
                                <p class="text-gray-500 mt-1">Gestiona el equipo de Uniendo Metas</p>
                            </div>
                            <a href="/admin/voluntarios/crear"
                                class="bg-blue-600 text-white font-bold px-6 py-3 rounded-xl hover:bg-blue-700 transition shadow-lg shadow-blue-200 flex items-center gap-2">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M12 4v16m8-8H4" />
                                </svg>
                                Nuevo Voluntario
                            </a>
                        </div>

                        <!-- Filters -->
                        <div class="bg-white p-4 rounded-2xl shadow-sm border border-gray-200 mb-8">
                            <form action="/admin/voluntarios" method="GET" class="flex flex-col md:flex-row gap-4">
                                <div class="flex-1">
                                    <input type="text" name="q" placeholder="Buscar por nombre..."
                                        value="<%= filters.q || '' %>"
                                        class="w-full px-4 py-2 rounded-xl border border-gray-300 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all">
                                </div>
                                <div class="w-full md:w-48">
                                    <select name="year"
                                        class="w-full px-4 py-2 rounded-xl border border-gray-300 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all bg-white">
                                        <option value="">Todos los años</option>
                                        <% years.forEach(y=> { %>
                                            <option value="<%= y %>" <%=filters.year==y ? 'selected' : '' %>><%= y %>
                                            </option>
                                            <% }) %>
                                    </select>
                                </div>
                                <button type="submit"
                                    class="px-6 py-2 bg-slate-800 text-white font-bold rounded-xl hover:bg-slate-700 transition">
                                    Buscar
                                </button>
                                <% if (filters.q || filters.year) { %>
                                    <a href="/admin/voluntarios"
                                        class="px-6 py-2 bg-gray-100 text-gray-600 font-bold rounded-xl hover:bg-gray-200 transition text-center flex items-center justify-center">
                                        Limpiar
                                    </a>
                                    <% } %>
                            </form>
                        </div>

                        <!-- Volunteers List Grouped by Year -->
                        <% if (volunteers.length> 0) { %>
                            <div class="space-y-8">
                                <% let currentYear=null; volunteers.forEach(volunteer=> {
                                    if (volunteer.year !== currentYear) {
                                    currentYear = volunteer.year;
                                    %>
                                    <!-- Year Separator -->
                                    <div class="border-b border-gray-200 pb-2 mt-8 first:mt-0">
                                        <h3 class="text-2xl font-black text-gray-400 flex items-center gap-2">
                                            <span class="bg-gray-100 px-3 py-1 rounded-lg text-gray-600 text-lg">
                                                <%= currentYear %>
                                            </span>
                                        </h3>
                                    </div>
                                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                                        <% } %>

                                            <!-- Volunteer Card -->
                                            <div
                                                class="bg-white p-6 rounded-2xl shadow-sm border border-gray-200 hover:shadow-md transition group relative">
                                                <div class="flex items-start justify-between">
                                                    <div>
                                                        <div
                                                            class="w-10 h-10 rounded-full bg-blue-50 text-brand-blue flex items-center justify-center font-bold mb-3">
                                                            <%= volunteer.name.charAt(0) %>
                                                        </div>
                                                        <h4 class="font-bold text-slate-800 text-lg mb-1 leading-tight">
                                                            <%= volunteer.name %>
                                                        </h4>
                                                        <p class="text-sm text-gray-500 font-medium">
                                                            <%= volunteer.role %>
                                                        </p>
                                                    </div>
                                                    <div
                                                        class="opacity-0 group-hover:opacity-100 transition-opacity absolute top-4 right-4 flex gap-2 bg-white/90 p-1 rounded-lg shadow-sm">
                                                        <a href="/admin/voluntarios/editar/<%= volunteer.id %>"
                                                            class="text-blue-600 hover:bg-blue-50 p-1.5 rounded-md transition">
                                                            <svg class="w-4 h-4" fill="none" stroke="currentColor"
                                                                viewBox="0 0 24 24">
                                                                <path stroke-linecap="round" stroke-linejoin="round"
                                                                    stroke-width="2"
                                                                    d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                                                            </svg>
                                                        </a>
                                                        <a href="/admin/voluntarios/eliminar/<%= volunteer.id %>"
                                                            onclick="return confirm('¿Eliminar?')"
                                                            class="text-red-500 hover:bg-red-50 p-1.5 rounded-md transition">
                                                            <svg class="w-4 h-4" fill="none" stroke="currentColor"
                                                                viewBox="0 0 24 24">
                                                                <path stroke-linecap="round" stroke-linejoin="round"
                                                                    stroke-width="2"
                                                                    d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                                            </svg>
                                                        </a>
                                                    </div>
                                                </div>

                                                <!-- End Grid if next item is different year or last item -->
                                                <% const nextVol=volunteers[volunteers.indexOf(volunteer) + 1]; if
                                                    (!nextVol || nextVol.year !==currentYear) { %>
                                            </div> <!-- End Grid -->
                                            <% } %>
                                    </div>
                                    <% }) %>
                            </div>
                            <% } else { %>
                                <div
                                    class="text-center py-20 bg-white rounded-2xl border border-dashed border-gray-300">
                                    <p class="text-gray-400 text-lg">No se encontraron voluntarios.</p>
                                    <% if (filters.q || filters.year) { %>
                                        <a href="/admin/voluntarios"
                                            class="text-brand-blue font-bold mt-2 inline-block hover:underline">Limpiar
                                            filtros</a>
                                        <% } %>
                                </div>
                                <% } %>
                    </div>
                </main>
        </div>

</body>

</html>