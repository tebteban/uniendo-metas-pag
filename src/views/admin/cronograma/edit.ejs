<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800;900&display=swap"
        rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
        }
    </style>
</head>

<body class="bg-slate-50">

    <%- include('../partials/navbar') %>

        <div class="pt-20 flex min-h-screen">
            <%- include('../partials/sidebar') %>

                <main class="flex-1 md:ml-64 p-8">
                    <div class="max-w-2xl mx-auto">
                        <div class="flex items-center gap-4 mb-8">
                            <a href="/admin/cronograma" class="text-gray-400 hover:text-slate-700 transition">
                                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                                </svg>
                            </a>
                            <h2 class="text-3xl font-black text-slate-800">Editar Actividad</h2>
                        </div>

                        <div class="bg-white p-8 rounded-2xl shadow-sm border border-gray-200">
                            <form action="/admin/cronograma/update/<%= event.id %>" method="POST" class="space-y-6">

                                <div class="grid grid-cols-2 gap-6">
                                    <div>
                                        <label for="day" class="block text-sm font-bold text-gray-700 mb-2">Día (Ej:
                                            Viernes)</label>
                                        <input type="text" id="day" name="day" value="<%= event.day %>" required
                                            class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all">
                                    </div>
                                    <div>
                                        <label for="date" class="block text-sm font-bold text-gray-700 mb-2">Fecha (Ej:
                                            4 de Octubre)</label>
                                        <input type="text" id="date" name="date" value="<%= event.date %>" required
                                            class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all">
                                    </div>
                                </div>

                                <div class="grid grid-cols-2 gap-6">
                                    <div>
                                        <label for="time" class="block text-sm font-bold text-gray-700 mb-2">Horario
                                            (Ej: 08:00 HS)</label>
                                        <input type="text" id="time" name="time" value="<%= event.time %>" required
                                            class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all">
                                    </div>
                                    <div>
                                        <label for="type"
                                            class="block text-sm font-bold text-gray-700 mb-2">Tipo</label>
                                        <select id="type" name="type"
                                            class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all bg-white">
                                            <option value="cap_autoridades" <%=event.type==='cap_autoridades'
                                                ? 'selected' : '' %>>Cap. Autoridades</option>
                                            <option value="cap_delegados" <%=event.type==='cap_delegados' ? 'selected'
                                                : '' %>>Cap. Delegados</option>
                                            <option value="sorteo" <%=event.type==='sorteo' ? 'selected' : '' %>>Sorteo
                                            </option>
                                            <option value="examen" <%=event.type==='examen' ? 'selected' : '' %>>Examen
                                            </option>
                                            <option value="convivencia" <%=event.type==='convivencia' ? 'selected' : ''
                                                %>>Convivencia</option>
                                            <option value="modelo" <%=event.type==='modelo' ? 'selected' : '' %>>Día de
                                                Modelo</option>
                                            <option value="general" <%=event.type==='general' ? 'selected' : '' %>
                                                >General</option>
                                        </select>
                                    </div>
                                </div>

                                <div>
                                    <label for="activity"
                                        class="block text-sm font-bold text-gray-700 mb-2">Actividad</label>
                                    <input type="text" id="activity" name="activity" value="<%= event.activity %>"
                                        required
                                        class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all">
                                </div>

                                <div>
                                    <label for="location"
                                        class="block text-sm font-bold text-gray-700 mb-2">Lugar</label>
                                    <input type="text" id="location" name="location" value="<%= event.location %>"
                                        class="w-full px-4 py-3 rounded-xl border border-gray-300 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all">
                                </div>

                                <div class="pt-4 flex justify-end gap-4">
                                    <a href="/admin/cronograma"
                                        class="px-6 py-3 rounded-xl text-gray-600 font-bold hover:bg-gray-100 transition">Cancelar</a>
                                    <button type="submit"
                                        class="px-8 py-3 rounded-xl bg-blue-600 text-white font-bold hover:bg-blue-700 transition shadow-lg shadow-blue-200">Actualizar
                                        Actividad</button>
                                </div>

                            </form>
                        </div>
                    </div>
                </main>
        </div>
</body>

</html>