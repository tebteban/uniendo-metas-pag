<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800;900&display=swap"
        rel="stylesheet">
    <style>
        body {
            font-family: 'Montserrat', sans-serif;
        }
    </style>
</head>

<body class="bg-slate-50">

    <%- include('../partials/navbar') %>

        <div class="pt-20 flex min-h-screen">
            <%- include('../partials/sidebar') %>

                <main class="flex-1 md:ml-64 p-8">
                    <div class="max-w-6xl mx-auto">

                        <div class="flex justify-between items-center mb-8">
                            <div>
                                <h2 class="text-3xl font-black text-slate-800">Equipo de Organización</h2>
                                <p class="text-gray-500 mt-1">Gestiona los integrantes y fotos del equipo</p>
                            </div>
                            <a href="/admin/equipodevoluntarios/crear"
                                class="bg-blue-600 text-white font-bold px-6 py-3 rounded-xl hover:bg-blue-700 transition shadow-lg shadow-blue-200 flex items-center gap-2">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M12 4v16m8-8H4" />
                                </svg>
                                Nuevo Miembro
                            </a>
                            <form action="/admin/equipodevoluntarios/importar" method="POST"
                                enctype="multipart/form-data" class="inline-block ml-2">
                                <label for="excel-upload"
                                    class="cursor-pointer bg-green-600 text-white font-bold px-6 py-3 rounded-xl hover:bg-green-700 transition shadow-lg shadow-green-200 flex items-center gap-2">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
                                    </svg>
                                    Importar Excel
                                </label>
                                <input id="excel-upload" type="file" name="file" accept=".xlsx, .xls" class="hidden"
                                    onchange="this.form.submit()">
                            </form>
                        </div>

                        <div class="bg-white rounded-2xl shadow-sm border border-gray-200 overflow-hidden">
                            <div class="overflow-x-auto">
                                <table class="w-full text-left border-collapse">
                                    <thead>
                                        <tr
                                            class="bg-slate-50 border-b border-gray-200 text-xs uppercase text-gray-500 font-bold tracking-wider">
                                            <th class="px-6 py-4">Orden / Perfil</th>
                                            <th class="px-6 py-4">Área</th>
                                            <th class="px-6 py-4">Rol</th>
                                            <th class="px-6 py-4">Estado</th>
                                            <th class="px-6 py-4">Bio</th>
                                            <th class="px-6 py-4 text-right">Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody class="divide-y divide-gray-100">
                                        <% let currentGroup='' ; authorities.forEach(auth=> {
                                            if(auth.group !== currentGroup) {
                                            currentGroup = auth.group;
                                            %>
                                            <tr class="bg-gray-50/50">
                                                <td colspan="5"
                                                    class="px-6 py-2 text-xs font-black text-gray-400 uppercase tracking-widest border-y border-gray-100">
                                                    <%= currentGroup || 'General' %>
                                                </td>
                                            </tr>
                                            <% } %>
                                                <tr class="hover:bg-slate-50 transition">
                                                    <td class="px-6 py-4 flex items-center gap-4">
                                                        <span class="text-gray-300 font-bold w-6 text-center text-sm">
                                                            <%= auth.order %>
                                                        </span>
                                                        <div
                                                            class="h-10 w-10 rounded-full bg-gray-200 overflow-hidden flex-shrink-0 border border-gray-300">
                                                            <% if(auth.image) { %>
                                                                <img src="/img/Voluntarios/<%= auth.image %>"
                                                                    class="w-full h-full object-cover">
                                                                <% } else { %>
                                                                    <svg class="w-full h-full text-gray-400 p-2"
                                                                        fill="currentColor" viewBox="0 0 20 20">
                                                                        <path fill-rule="evenodd"
                                                                            d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z"
                                                                            clip-rule="evenodd" />
                                                                    </svg>
                                                                    <% } %>
                                                        </div>
                                                        <span class="font-bold text-slate-700">
                                                            <%= auth.name %>
                                                        </span>
                                                    </td>
                                                    <td class="px-6 py-4 text-sm font-medium text-gray-500">
                                                        <%= auth.group %>
                                                    </td>
                                                    <td class="px-6 py-4">
                                                        <span
                                                            class="bg-indigo-50 text-indigo-700 px-3 py-1 rounded-full text-xs font-bold whitespace-nowrap">
                                                            <%= auth.role %>
                                                        </span>
                                                    </td>
                                                    <td class="px-6 py-4">
                                                        <form action="/admin/equipodevoluntarios/toggle/<%= auth.id %>"
                                                            method="POST">
                                                            <button type="submit"
                                                                class="<%= auth.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500' %> px-3 py-1 rounded-full text-xs font-bold whitespace-nowrap hover:opacity-75 transition">
                                                                <%= auth.active ? 'Publicado' : 'Oculto' %>
                                                            </button>
                                                        </form>
                                                    </td>
                                                    <td class="px-6 py-4 text-xs text-gray-400 max-w-xs truncate">
                                                        <%= auth.description %>
                                                    </td>
                                                    <td class="px-6 py-4 text-right space-x-2 whitespace-nowrap">
                                                        <a href="/admin/equipodevoluntarios/editar/<%= auth.id %>"
                                                            class="text-indigo-600 hover:text-indigo-900 font-bold text-sm">Editar</a>
                                                        <a href="/admin/equipodevoluntarios/eliminar/<%= auth.id %>"
                                                            class="text-red-500 hover:text-red-700 font-bold text-sm ml-2"
                                                            onclick="return confirm('¿Eliminar a <%= auth.name %>?')">Eliminar</a>
                                                    </td>
                                                </tr>
                                                <% }) %>
                                    </tbody>
                                </table>
                            </div>
                            <% if (authorities.length===0) { %>
                                <div class="p-12 text-center text-gray-400">
                                    <p>No hay miembros del equipo registrados aún.</p>
                                </div>
                                <% } %>
                        </div>

                        <!-- Publish All Button -->
                        <!-- Actions Footer -->
                        <div
                            class="mt-8 flex justify-between items-center bg-slate-50 p-4 rounded-xl border border-slate-200">
                            <form action="/admin/equipodevoluntarios/vaciar" method="POST"
                                onsubmit="return confirm('⚠️ PELIGRO: ¿Estás seguro de que quieres VACIAR toda la lista? Esta acción eliminará a TODOS los miembros y NO se puede deshacer.');">
                                <button type="submit"
                                    class="bg-red-100 text-red-700 font-bold px-6 py-3 rounded-xl hover:bg-red-200 transition border border-red-200 flex items-center gap-2">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none"
                                        viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                    </svg>
                                    Vaciar Lista
                                </button>
                            </form>

                            <form action="/admin/equipodevoluntarios/publicar-todos" method="POST"
                                onsubmit="return confirm('¿Estás seguro? Esto hará visible a TODO el equipo en la página pública.');">
                                <button type="submit"
                                    class="bg-green-600 text-white font-bold px-8 py-3 rounded-xl hover:bg-green-700 transition shadow-lg shadow-green-200 flex items-center gap-2">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M5 13l4 4L19 7" />
                                    </svg>
                                    Publicar Equipo Completo
                                </button>
                            </form>
                        </div>

                    </div>
                </main>
        </div>

</body>

</html>