<link rel="icon" type="image/png" href="/img/descarga.png">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof titulo != 'undefined' ? titulo : 'Uniendo Metas Santiago del Estero' %></title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700;900&display=swap" rel="stylesheet">
    
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

    <script src="https://cdn.tailwindcss.com"></script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { sans: ['Montserrat', 'sans-serif'] },
                    colors: {
                        brand: {
                            red: '#A02140',      // Consejo de Seguridad
                            green: '#73A950',    // Asamblea General
                            yellow: '#FFB819',   // Sala de Tratados
                            orange: '#E15829',   // ECOSOC
                            blue: '#61B4E4',     // CDH / Subsidiario
                            gray: '#8A8A8D',     // Texto General
                            dark: '#1A202C'
                        }
                    },
                    animation: {
                        'float': 'float 6s ease-in-out infinite',
                    },
                    keyframes: {
                        float: {
                            '0%, 100%': { transform: 'translateY(0)' },
                            '50%': { transform: 'translateY(-20px)' },
                        }
                    }
                }
            }
        }
    </script>

    <style>
        /* Trama Poligonal (Simulación CSS) */
        .polygonal-bg {
            background-image: linear-gradient(135deg, #fdfbfb 0%, #ebedee 100%);
            position: relative;
        }
        .polygonal-bg::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%239C92AC' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
        }
        
        .card-hover {
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        .card-hover:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px -5px rgba(0, 0, 0, 0.1);
        }
        
        /* Botón primario con gradiente institucional */
        .btn-primary {
            background: linear-gradient(135deg, #A02140 0%, #E15829 100%);
            box-shadow: 0 4px 15px rgba(160, 33, 64, 0.4);
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(160, 33, 64, 0.5);
        }

        /* Animación para el carrusel infinito */
@keyframes scroll-infinite {
    0% { transform: translateX(0); }
    100% { transform: translateX(-50%); }
}

.animate-scroll {
    display: flex;
    width: max-content; /* Asegura que el ancho sea la suma de los hijos */
    animation: scroll-infinite 30s linear infinite; /* 30s determina la velocidad */
}

/* Pausar la animación si pasan el mouse por encima (opcional) */
.animate-scroll:hover {
    animation-play-state: paused;
}
    </style>
</head>